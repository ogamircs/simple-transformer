[project]
name = "simple-transformer"
version = "0.1.0"
description = "A transformer built from scratch for learning, inspired by karpathy/nanochat"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.0",
    "numpy",
    "requests",
]
